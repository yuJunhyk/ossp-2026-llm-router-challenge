# v1.5 실험 게이트 — 사전 등록 (2026-08-18, 실행 전 고정)

> v1.4의 build/v14/gates.md와 같은 역할이되, build/는 gitignore라 로컬 전용이었으므로
> 이번에는 추적 파일로 격상한다. 이 파일의 최초 커밋 타임스탬프가 곧 사전 등록 증명이다.
> 실험은 전부 lab 클론(v15-experiments 브랜치)에서 수행하며 원본 저장소는 병합 시점까지 무접촉.

## 복귀점과 기준선

- 복귀점: 원본 HEAD ba0953236b005868c7712179d204511558daa202,
  아티팩트 sha256 b2f1e3a89dde26cee29ec98f08cd8535e6ce907d987e370bd426a0e254c4745d
- 기준선: linear(λ=10) train-only 그룹 5-fold × seed(11,22,33) weighted CV = **0.6553**
- fold 정의·캘리브레이션 격자(BETAS, MARGINS)·SAFETY_CAP·STRESS_CAP은 rematch.py 현행 그대로. 변경 금지.
- **판정에 dev 점수 사용 금지** (0.6759 등 일체). dev 데이터·채점은 최종 승자 확정 후 1회만 접근.

## 스크리닝 게이트 (후보별 독립, 동일 fold 짝지음 비교)

| 후보 | 실험 | 통과 기준 | 결과 |
|---|---|---|---|
| C2 | 형태소 근사 어간 채널 추가 (salt "s", 조사 제거, 어간 ≥2) | ΔCV ≥ +0.004 | **미달** — 0.6524, Δ=-0.0029, per-fold 2/15. 기각 |
| C5 | 라벨 보존 표기 증강 (키 상속, va는 원본만, λ=10 고정) | ΔCV ≥ +0.004 | **미달** — 0.6535, Δ=-0.0018, per-fold 1/15 (증강 322개뿐). 기각 |
| C1 | 템플릿 유사도 특징 (fold-train 은행, 캡/이진화, 수치 슬롯 ≤6) | ΔCV ≥ +0.004 | **미달** — 0.6563, Δ=+0.0011, per-fold 9/15, 고정 캘리브레이션 Δ=-0.0024. 기각 (CV는 배포 이득의 하한이나 게이트 유지) |
| C3 | 점수 헤드 dual IRLS 로지스틱 (분수 타깃, binomial weight=생성 수, λ_logit ∈ {3, 10, 30}) | ΔCV ≥ +0.004 | |
| C4 | 비용 이분산 σ_i — **조건부** (C1·C2·C3·C5 완료 후 시간 잔여 시에만) | 선행 관문: calibrate fork가 σ_i≡상수에서 0.6553 재현. 본 게이트: ΔCV ≥ +0.004 | |

- 보조 서술 기준(게이트 아님): paired per-fold Δ를 report JSON에 기록. "15 fold 중 ≥10에서 Δ>0"을 참고 지표로 병기. seed 간 비독립이므로 형식적 p값 사용 금지.
- 진단 전용(게이트 아님): v1.3 고정 (β,margin) = fast(0.5, 0.90) / bal(0.5, 0.96) / prem(1.0, 0.80)에서의 Δ를 병기해 예측 개선분과 캘리브레이션 이동분을 분해.
- 각 exp 스크립트는 변형 적용 전 기준선 0.6553을 동일 fold에서 재현해야 유효.

## 조합·통합 게이트

- 통과자 k=0: 전량 폐기, v1.3 확정.
- k=1: 해당 후보가 곧 최종 후보.
- k=2: 조합 1런. **조합 CV ≥ max(개별) + 0.002**이면 조합, 미달 시 최고 개별. 동률이면 단순한 쪽.
- k=3: 조합 + 페어 3런(총 4런). 같은 규칙으로 최고 구성 채택.
- **통합 게이트: 최종 배포 후보 weighted CV ≥ 0.6613 (기준선 +0.006).** 미달 시 전량 폐기, v1.3 확정.

## Dev 게이트 규칙 (최종 승자 1회)

- 네트워크 차단 컨테이너에서 3-tier self-check 1회.
- **판정 기준은 예산 초과 여부만.** 세 tier 모두 한도 내면 점수와 무관하게 그 구성을 제출한다.
- 점수는 기록만 하며 어떤 선택의 근거로도 쓰지 않는다 (v1.3 반사실 분석과 동일 취지 명문화).
- 예산 초과 시에만 v1.3 복귀 (객관적 사유, 오염 아님).

## 체크포인트 탈락 규칙

- CP1 (8/20 저녁): 이 시점까지 스크리닝 미완 후보는 자동 탈락. C4 착수 여부 판정.
- CP2 (8/22): 조합 승자 확정. C1이 승자 조합에 포함됐는데 배포 배관(은행 아티팩트·parity 재설계) 미완이면 C1 제외 차선 조합으로 전환.
- CP3 (8/24 정오, 하드): 새 아티팩트가 컨테이너 검증(check_runtime 3-tier) 미통과면 CV Δ와 무관하게 v1.3 확정.

## 산출물 규약

- 각 실험: `build/v15/<name>.json`에 weighted·delta·per-fold Δ·pass·git commit hash 기록.
- 영구 기록: analysis/REMATCH.md 실험 표 (build/는 gitignore이므로 여기 미기록 시 증발).
- 병렬 실행 허용. BLAS 스레드 제한(프로세스당 4), 실행 중 스크립트 편집 금지.
