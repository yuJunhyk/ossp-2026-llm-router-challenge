<!--
SPDX-FileCopyrightText: Copyright 2026 yuJunhyk
SPDX-License-Identifier: Apache-2.0
-->

# v1.6 실험 게이트 — 사전 등록 (2026-08-18, 공식 실행 전 고정)

> v1.5와 동일 규약. 이 파일의 커밋 타임스탬프가 사전 등록 증명이다.
> 실험은 lab 클론 v16-experiments 브랜치에서, 원본 저장소는 병합 시점까지 무접촉.
> **dev 접근 금지** — 최종 승자 확정 후 1회(예산 판정 전용)만.

## 복귀점과 기준선

- 복귀점: 원본 HEAD ee7b677b2c11233623c210cb4e4485b8695f8f62,
  아티팩트 sha256 b2f1e3a89dde26cee29ec98f08cd8535e6ce907d987e370bd426a0e254c4745d
- 기준선: linear(λ=10) 그룹 5-fold × seed(11,22,33) weighted CV = **0.6553**
- 각 스크립트는 변형 전 기준선 재현(오차 ≤ 0.0005) + 변형 항등원(γ=1 / τ→0)에서
  기준선과의 비트 동일성 확인을 선행 관문으로 둔다.

## 투명성 고지 — 가설의 출처

후보 A의 구체 형태(uplift 축소)는 **train 전용 읽기 탐색**(설계 검증 에이전트,
12개 변형 arm)에서 발견됐다. dev는 그 과정에서 일절 접근하지 않았다. V15-GATES가
허용한 train-CV 기반 적응이며, 아래에 등록하는 것은 탐색의 최대값이 아니라
사전 지정한 주 arm 하나다. 탐색 결과 전문은 결과 JSON에 참고로 병기한다.

또한 후보 C(구조 난이도 특징)는 사전 스크리닝에서 기각됐음을 기록한다:
26개 특징 전부 기존 모델 잔차와의 상관 |r| < 0.05 (최대 +0.044). 특징 축은
경험적으로 닫혔다. 세 번째 슬롯은 사용자 승인으로 A×B 조합으로 대체됐다.

## 후보 A — 3.1 uplift 축소 (γ family)

배경 실측: 예측된 L→M 점수 격차의 참값 상관은 0.033 (M→K는 0.412). 상관 없는
격차 예측이 그리디 효율 순위에 들어가면 과대예측 문항이 우선 승급되는 역선택이
생긴다. 변환 ŝ_M' = ŝ_L + γ·(ŝ_M − ŝ_L) + (1−γ)·ḡ_tr (ḡ_tr = fold-train의
참 평균 uplift)로 그 노이즈를 죽인다. γ=1이 항등원(기준선 비트 동일).

- **주 arm: γ = 0** (격차 완전 제거 — 3.1 승급은 비용 순으로만 정렬됨)
- **헤지 arm: γ = 0.25** (신호 25% 보존 — 분포 이동 대비, 주 arm 실패 시에만 승계)
- 참고 arm(판정 자격 없음): 정직 inner-CV(그룹 5-fold, seed 7)로 적합한
  C&W 3×3 점수 블록 M — "적합된 M이 flatLM을 근사한다"는 귀속 주장 검증용
- 이중 게이트 (둘 다 필요): ① calibrate ΔCV ≥ +0.004
  ② v1.3 고정 (β,margin)에서 Δ ≥ 0 그리고 15-fold 예산 위반 0건
- 반복 검증(보고 전용): 독립 fold 구조 seed(44,55,66)에서 같은 arm의 Δ 병기
- 채택 규칙: 주 arm이 이중 게이트 + 통합 게이트(≥ 0.6613)를 모두 넘으면 주 arm.
  주 arm이 실패하고 헤지 arm이 전부 넘으면 헤지 arm. 그 외 기각.

| arm | 결과 |
|---|---|
| γ=0 (주) | **통과** — 0.6621, Δ=+0.0068, 고정Δ=+0.0045, 위반 0, per-fold 12/15. 통합 게이트(0.6613)도 충족 |
| γ=0.25 (헤지) | 통과(승계 불요) — 0.6594, Δ=+0.0041, 고정Δ=+0.0052, 위반 0, per-fold 13/15 |
| C&W 3×3 (참고) | 0.6611, Δ=+0.0058 — 적합 M이 flatLM을 근사한다는 귀속 확인 |

## 후보 B — 결정 정렬 손실 (SPO+ 점수 헤드 미세조정)

- 정식화: tier별 λ_t = fold-train 참 (점수,비용) 이분 탐색 그림자가격 (목표
  지출비 = v1.3 고정 설정의 fold-train 실현 지출비). λ 지터 {0.8, 1.0, 1.25} 고정.
- 파라미터화: dual 신뢰영역 Δ = X_trᵀA, RMS(K_tr A) ≤ τ. **τ=0.01 단독 판정**
  (0.005·0.02는 민감도 보고 전용, 후보 자격 없음). T=200, 후반 100 Polyak 평균,
  η는 초기 기울기 정규화 — 튜닝 없음. 점수 3열만 조정, 비용·smear·σ 무변경.
- **킬 스위치**: seed 11 예비 학습에서 OOF corr(예측 3.1 uplift, 참값) 개선이
  +0.02 미만이면 본 실험 중단, 기각 기록. (사전 실측: 특징 공간 내 3.1 uplift
  선형 상관 상한 ~0.03, 게이트 도달에 Δcorr≈+0.06 필요)
- 이중 게이트: 후보 A와 동일 (① +0.004 ② 고정 Δ≥0 ∧ 위반 0)

| 단계 | 결과 |
|---|---|
| 킬 스위치 (Δcorr ≥ +0.02) | **발동** — corr 0.0313→0.0342, Δ=+0.0029. 본 실험 중단 |
| 본 게이트 | 미실행 (킬 스위치) — 결정 정렬 축도 신호 부재로 도달 불가 판정. 기각 |

## 조합 A×B (세 번째 슬롯, 사용자 승인 2026-08-18)

- A·B 모두 이중 게이트 통과 시에만 실행: A 변환을 적용한 점수 헤드를 warm start로
  B의 SPO+ 미세조정. 채택은 조합 ≥ max(A, B) + 0.002, 아니면 최고 단독.
- 한쪽만 통과하면 그 단독 구성이 최종 후보. 둘 다 미달이면 캠페인 종결, v1.3 확정.

## 반복 검증·조합 기록

- 반복(seed 44/55/66, 보고 전용): γ=0 Δ=+0.0041, γ=0.25 Δ=+0.0038 — 독립 fold
  구조에서 효과 재현. 표시된 위반 2건은 기준선도 동일하게 2건(fold 구조 성질,
  v1.3 margin이 다른 구조에서 캘리브레이션된 탓)이라 변형 귀책 아님을 실측 확인.
- 조합 A×B: 미실행 — B가 킬 스위치로 기각되어 등록 조건(양쪽 통과) 불충족.
- **최종 후보: γ=0 (3.1 격차 완전 제거).** 항등원 검사 5.6e-17, 배정 이동률
  fast 16.7% / bal 11.8% / prem 12.1%.

## 통합 게이트와 채택 절차

- 최종 배포 후보 weighted CV ≥ **0.6613** (기준선 +0.006).
- 채택 시: 배포용 변환을 가중치에 접기(W'[:,1] = γ·W[:,1] + (1−γ)·W[:,0],
  bias 슬롯 33에 (1−γ)·ḡ_train 가산 — 런타임 무변경) → train_linear.py의
  자체 대조 2곳 갱신 → 아티팩트 재생성 → pytest 전수 → check_runtime 3-tier
  → Dev 1회(예산 초과 여부만 판정, 점수는 기록 전용) → 병합.
- Dev 예산 초과 시에만 v1.3 복귀. **push는 매 단계 사용자 승인 후에만.**

## 산출물 규약

- `build/v16/{uplift-shrink,spo-plus,combo}.json` — weighted·Δ·per-fold·고정 진단·
  누수 자가검사·git hash 기록. 영구 기록은 REMATCH.md.
- 누수 자가검사: 모든 추정(ḡ_tr, M, λ_t, SGD)이 fold-train 슬라이스만 소비함을
  함수 시그니처와 assert로 강제하고 JSON에 명기.

## Dev 게이트 결과 (2026-08-18, 등록 규칙대로 1회 실행)

네트워크 차단 컨테이너(ossp-router:v16, --network none)에서 Dev 3-tier 생성,
공식 self-check 채점. **판정 기준은 예산 초과 여부만이다.**

| tier | 예산 | 판정 | 배정 L/M/K |
|---|---|---|---|
| fast | 1.106 / 1.25 (88.5%) | 통과 | 559 / 321 / 0 |
| balanced | 1.697 / 2.0 (84.8%) | 통과 | 100 / 773 / 7 |
| premium | 2.428 / 4.0 (60.7%) | 통과 | 86 / 744 / 50 |

**세 tier 전부 예산 통과 → 등록 규칙에 따라 v1.6을 제출 구성으로 확정한다.**

점수는 기록 전용으로 남긴다: final 0.6730 (fast 0.6346 / bal 0.6892 / prem 0.7079).
v1.3의 0.6759와의 차이 -0.0029는 이 프로젝트가 실측한 CV 노이즈(±0.5pp)와
평가셋 간 순위 요동(5.4절 반사실: 근소 동률 후보의 순서는 평가셋마다 뒤집힌다)
범위 안이며, **이 비교를 근거로 구성을 되돌리면 그것이 곧 v1.2식 dev 오염의
재발이므로 사전 등록 규칙을 유지한다.** train CV는 +0.0068로 v1.6이 우위이고,
fast 예산 사용률이 93.6%→88.5%로 내려가 비공개셋 비용 이동 내성이 +6.9%에서
+13.0%로 늘었다는 것이 부수 이득이다.

dev 사용 이력 갱신: v1.3 게이트 1회 + 회고 2회 + **v1.6 게이트 1회 (본 실행)**.
선택 개입은 여전히 0회다.

검증 사슬: 항등원 5.6e-17 → 아티팩트 재생성(런타임 대조 1.78e-15) → pytest 271
통과(upstream 기존 1건만) → check_runtime 3-tier PASS(5.5~5.8초/90초) → Dev 예산 통과.

## 기록 전용 진단 — Premium 잔여 예산의 해부 (2026-08-18, 게이트 아님)

질문: v1.6 Premium이 Dev에서 예산의 60.7%만 쓰는 이유가 예산 소진(β 비관)인지
후보 소진(K1 증분 미생성)인지. **train 전용·회고 진단이며 채택 대상이 없다 —
dev 미사용, 선택 개입 0회, 구성 변경 없음.**

- 스크립트: `analysis/eda_premium_stop.py` → `build/v16/premium-stop-diag.json`
  (git 39f1069, 아티팩트 sha256 bbb87f13…)
- 선행 관문: 계측 fork가 원본 `os2_policy.allocate`와 전 실행(deployed 1 +
  CV 30회) choice 완전 일치 — 불일치 0.
- 방법 주의: allocate 루프에는 break가 없어 "멈춘 시점"이 존재하지 않는다.
  증분 단위 기각 사유(채택 / 예산 skip / 선행 스텝 연쇄 skip)로 집계했다.

전체 train 1,760 × 배포 아티팩트(β=1.0, margin=0.92, mult=4.0):

| 축 | 실측 |
|---|---|
| K1 증분 미생성 (후보 소진) | 508문항 (28.9%) — m_only 502 + steps 없음 6 |
| K1 증분 예산 기각 | 1,156문항 (65.7%) — 직접 1,067 + 선행 3.1 연쇄 89 |
| K1 채택 | 96문항 (5.5%) |
| 비관 예산 소진율 | **99.98%** (spent 32.318 / budget 32.325) |
| train 실현 사용률 | 2.210 / 4.0 (55.3%) — Dev 실현 60.7%와 같은 층위 |
| 비관 쐐기 | exp(β·σ_K1)=1.963, smear_K1=1.325 |
| 상수 헤드 검증 | ŝ_M−ŝ_L 최대 0.08125, 상수 이탈 28건(클리핑) — `s_k>s_a`가 고정 문턱으로 축퇴 |

15-fold CV 대조 (median [min, max]):

| 구성 | K1 미생성률 | 비관 소진율 | 실현 사용 |
|---|---|---|---|
| v1.6 (γ=0, β=1.0, m=0.92) | 28.7% [19.6, 38.4] | 99.89% | 2.550 [1.962, 3.541] / 4.0 |
| v1.3 대조 (원 헤드, β=1.0, m=0.80) | 23.9% [16.2, 34.7] | 99.98% | 2.158 [1.738, 3.478] / 4.0 |

판정: **두 가설이 동시에 참이되 지배 축은 예산 소진이다.** K1 승급 후보
1,252건 중 1,156건이 예산 사유로 기각됐고 allocate는 자기 기준(비관 비용)
예산을 99.98%까지 소진한다 — 라우터는 지갑을 다 연 것이고, 실현 사용률이
낮은 것은 그 잣대가 β 쐐기로 K1 비용을 1.96배 부풀려 계산하기 때문이다.
부차 축으로 28.9%는 후보 자체가 안 생긴다: 상수 헤드가 만든 고정 문턱
(ŝ_K−ŝ_L > 0.08125)이 실질 게이트이며, 이는 "승급 상승분을 가려낼 신호가
라벨 수 안에 없다"는 진단의 K1 축 실증이다. 결과는 결과보고서 8절 한계
서술에만 쓰인다.
